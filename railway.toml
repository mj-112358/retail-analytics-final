[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile.final.production"

[deploy]
startCommand = "uvicorn main_final:app --host 0.0.0.0 --port ${PORT}"
healthcheckPath = "/health"
healthcheckTimeout = 120
restartPolicyType = "always"